<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <!-- This is the stroke you want to define -->
    <stroke android:width="5dp"
        android:color="@color/light_purple"/>

    <!-- Optional, round your corners -->
    <corners android:bottomLeftRadius="35dp"
        android:topLeftRadius="35dp"
        android:bottomRightRadius="35dp"
        android:topRightRadius="35dp" />

    <!-- Optional, fill the rest of your background with a color or gradient, use transparent if you only want the border to be displayed-->
    <gradient android:startColor="@color/fondo_popup"
        android:endColor="@color/fondo_popup"
        android:angle="90"/>
</shape>